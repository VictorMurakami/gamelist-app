CREATE TABLE TextPrefEntity (
    pref_key TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);

get:
SELECT value FROM TextPrefEntity WHERE pref_key = ?;

upsert:
INSERT OR REPLACE INTO TextPrefEntity(pref_key, value) VALUES (?, ?);

delete:
DELETE FROM TextPrefEntity WHERE pref_key = ?;
